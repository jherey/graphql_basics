// GraphQL uses the Schema Definition Language (DSL)

// DEFINING SIMPLE TYPES - Exclamation sign (!) signifies required fields

type Person {
  name: String!
  age: Int!
}

type Post {
  title: String!
}
